Building and Running:
    Create a build directory: mkdir build && cd build
    Configure CMake: cmake ..
    Build the project: cmake --build .
    Run the application: ./MyQtApp (or the name you specified in add_executable)

Start Designer:
    /usr/lib/qt6/bin/designer



export $(cat .env | xargs) && ./practice1_server

practice server
build/ :
cmake .. 
make practice1_server
        