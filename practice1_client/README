First Building and Running:
    mkdir build && cd build
    cmake ..
    cmake --build .
    ./practice1 or export $(cat .env | xargs) && ./practice1_server

Start Designer:
    /usr/lib/qt6/bin/designer
     